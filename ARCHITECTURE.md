# 構成と保守ガイド

## 読み込みと責務

`index.html` が `styles.css` を読み込み、`board-model.js` → `app.js` の順に `defer` で実行します。アプリの実行には外部ライブラリもビルドも不要です。ファイルを直接開く利用を維持するため、ES Modulesではなく通常のスクリプトとして読み込みます。

`board-model.js` は `BoardModel` を公開します。Node.jsでは同じ関数を `module.exports` から参照でき、テストのためにJavaScriptのソース文字列を切り出す必要はありません。

| モデルの関数           | 責務                                               |
| ---------------------- | -------------------------------------------------- |
| `createInitialState()` | 両チーム12人・ボール・空の矢印配列を新規作成       |
| `createDefaultSlots()` | 基本配置・作戦A/B・左右反転の5枠を新規作成         |
| `isValidState()`       | 保存形式、駒の識別子・人数・座標、矢印の形式を検証 |
| `aimPolygon()`         | 選手の円による遮蔽を考慮した狙い範囲の頂点を計算   |

`app.js` は画面と入力を担当します。順に、操作定数と状態、通知・説明、履歴、座標変換・描画、倍率・表示方向、コート操作、保存枠、キーボード、初期化を配置しています。`TIMING` と `LIMITS` には操作時間・主要な判定距離・倍率上限・履歴上限を集約しています。

## 状態とデータの寿命

- `state`：駒と残す矢印。コピーを履歴に記録します。
- `recalledSlotIndex`：最後に呼び出した配置枠。`初期位置へ`では、この番号の枠をlocalStorageから読み直して最新内容を適用します。再読み込みでは保持しません。
- `temporaryArrows`：一時矢印と消去期限。保存枠・履歴には含めません。
- `gestures`：pointerIdごとの操作。駒の元位置、長押しタイマー、指の位置、描画プレビューなどを持ちます。
- `landscape` / `zoom` / `pinch`：表示方向、倍率、複数指操作の基準。
- `undo` / `redo`：配置の履歴。複数指による一連の操作は最後の指を離したときに1操作として記録します。
- localStorage：登録済みの配置枠だけを保持します。表示状態や操作途中の情報は再読み込みで復元しません。

配置モデルは毎回新しいオブジェクトを返します。保存枠や履歴を適用するときもコピーし、現在の駒を動かしただけで保存内容まで変わることを避けています。

## 座標と描画

保存座標は常に縦向きの百分率です。左上が `(0, 0)`、右下が `(100, 100)`、味方側は下です。競技境界はx=7〜93、y=5〜95、ネットはy=50です。横表示への変換は `screenPoint()`、入力から保存座標への逆変換は `courtPoint()` が担当します。

コート描画領域の縦横比は45:86です。競技境界の幅86%・高さ90%と組み合わせて9m×18mになります。`updatePieceSize()` は競技領域の幅から前衛は直径75cm・後衛は直径100cmの選手と直径50cmのボールをそれぞれ計算します。

選手の円は、相手のアタックに対するレシーブ可能範囲の目安です。相手に近い前衛は両ひざの横あたりまで、後衛はもう少し手を広げて対応できるため、円の大きさを変えています。

HTMLの重なりはコート線、狙い範囲のCanvas、矢印のSVG、駒の順です。`drawAim()` は画面密度に合わせたCanvasで表示し、計算自体は向きに依存しない等方座標の `aimPolygon()` に渡します。

`render()` は駒を作り直し、移動範囲への補正・矢印・履歴ボタン・狙い範囲を更新します。ドラッグ中は対象の駒だけを `placePiece()` で動かし、ポインター操作を維持します。

## 入力処理

`startCourtGesture()` → `moveCourtGesture()` → `finishCourtGesture()` が操作の開始・移動・終了を担当します。

1. 駒への入力は直ちにドラッグを開始します。選手はドラッグ中はエリア制限なしの連続座標で動き、離すと240msで25cmグリッドまたはライン接触位置へスナップし、ボールは連続座標で動きます。同じ駒は最初の指を優先します。
2. 表示中の矢印が1本以上ある場合、空いたコートは直ちに描画を開始します。矢印が0本の場合は長押し待機に入り、約500ms後に描画が有効になります。
3. 矢印が0本で、長押し前に移動の閾値を超えた場合は待機を取り消してパンします。長押し後に矢印を確定できなかった場合も、次回は長押し待機から始めます。
4. 空いたコートに2本以上の指が触れた場合は描画を取り消し、重心移動でパン、指の広がりで倍率を変更します。駒を操作する指は本数に含めません。
5. 複数指から1本になった場合はパンを継続し、その操作中は矢印描画に戻しません。次の単独操作では、矢印が残っていれば直ちに描画し、0本なら長押しを待ちます。
6. 終了時は長押しタイマーを解除します。操作中断はその指の駒の移動を取り消し、通常終了は矢印や履歴を確定します。

一時矢印を確定するたびに、表示中の一時矢印の期限を同じ「現在時刻＋5秒」に更新します。`scheduleExpiry()` は期限到達時にまとめて消去します。描画開始時に `updateArrowExpiryPause()` が消去を停止し、`arrowExpiryPaused` が有効な間はタイマーと描画時の期限フィルターの両方で線を保持します。停止前に期限切れの線は復活させません。最後の描画が終了・中断した時、または複数指パンへ切り替わった時は既存線の期限を「現在時刻＋5秒」に揃えて再開します。「残す」の矢印を描いている間も一時線を保持します。狙い範囲の5秒タイマーは別管理です。

短いタップはダブルタップ判定に使います。長押し描画、ドラッグ、複数指操作はダブルタップとして扱いません。ツールを閉じるためのタップはコートに伝播させず、説明ダイアログ表示中は背景操作を抑止します。

## 表示とCSS

ページ全体のスクロールを止め、固定ツールバーの下の `.court-scroll` だけをスクロールします。CSSのコンテナ単位はこの表示領域を基準にします。外周20pxずつの余白は `minimumZoom()` の計算と対応するため、変更時は両方を見直してください。

`--court-zoom` は表示倍率、`--piece-size` は選手の直径です。コート内の `touch-action: none` は独自の長押し・パン・ピンチ判定に必要です。駒以外の重ね合わせ要素は原則として入力を透過します。

起動時は画面比率で縦横を選び、長辺合わせで全体を表示します。利用者による縦横切替は100%に戻します。倍率の適用は `applyZoom()`、1本指の移動は `panCourt()`、自コートへのスクロールは `showHomeCourt()` にまとめています。

CSSには基本書式、コートと縦横差分、ツール、状態表示、スクロール領域、説明ダイアログのコメントがあります。メディア条件とセレクターの優先順位を維持し、無条件に同じ指定へ上書きされる宣言や使用しない旧画面用セレクターを整理しています。

## 保存互換性とオフライン

保存キーは `rv-tactic-board-v1-slots`、旧1枠キーは `rv-tactic-board-v1` です。新形式がない場合に限り旧形式を読み込みます。旧形式との互換性のため、保存データの `opponents` と `arrows` フィールドも維持します。現在の配置登録は駒だけを保存し、矢印配列は空にします。

`sw.js` の `ASSETS` に公開ファイルを列挙しています。ファイルを追加する際は読み込み元だけでなく、この一覧も更新してください。公開ファイルを変更したら `CACHE` のバージョンを増やします。同一scopeの旧キャッシュだけを削除し、他アプリのキャッシュを残します。既存画面を強制更新せず、旧画面を閉じた後の更新を維持します。

`manifest.webmanifest` はJSONなのでコメントを入れません。起動先とscopeはサブディレクトリ公開を維持する相対パスです。`ball.svg` と `icons/` は画像資産です。

## 変更時の確認

```sh
node --test aim.test.cjs model.test.cjs browser.test.cjs
```

モデルと計算のテストはNode.jsだけで動きます。ブラウザテストはPlaywrightとMicrosoft Edgeを使い、ローカルサーバー、タッチ操作、保存、履歴、表示サイズ、説明ダイアログ、オフライン再読み込みを検証します。

操作の仕様を変えたら `index.html` の使い方、`README.md`、本書の関連箇所を更新してください。見た目の変更はスマホ縦・横とPCの両方向で確認します。実機固有の長押しや複数指操作は、ブラウザ自動テストとは別に確認が必要です。

## 自動位置の計算と表示

`selectAutoAnchors()` は両チームの左右の固定選手を選びます。`autoPosition(pieces, anchors, mode)` はボールの反対側について、ONでは固定2人を除く4人、FULLでは6人全員を対象に、前後をアタックライン接触位置、左右を25cm候補にします。実寸mで円の接線角度を計算する `autoGapScore()` により、最大の空き角度、空き角度の合計、各隙間の二乗和、移動量の順に比較します。黄色の塗り面積そのものではなく、ボールから見た通せる方向を評価します。

探索は各段階で96候補を残す幅制限探索で、数学的な大域最適解の保証はありません。枝刈りは移動量に依存させず、同じボール位置で再計算しても配置が往復しないようにします。現在の左右位置が有効なら最終比較にも残します。候補がなくなった場合は空配列を返し、呼び出し側で配置を維持して案内します。モデル関数は入力を書き換えません。

`autoMode`（off/on/full）、`autoEnabled` と `autoAnchors` は保存しません。ON・FULLへの切替時と手動操作完了時に固定選手を選び直し、ボールの座標が変わったときだけ `runAutoPosition()` を実行します。ON・FULLでも両チーム全選手の手動操作とボールの同時操作を許可します。選手のジェスチャーが1件でもある間、`runAutoPosition()` は再計算を行いません。選手の操作終了だけでは再計算せず、その後の次のボール移動で再開します。

`state` は確定した移動先を保持し、`autoAnimations` が表示中の座標を管理します。`visualPiece()` の座標を駒と狙い範囲の両方で使用します。requestAnimationFrameで240〜600msの移動を描画し、途中の更新は表示中の座標から再開します。駒のDOMを作り直してもアニメーションは継続します。履歴にはボールと選手の移動先をまとめて記録し、取り消し時はアニメーションを破棄します。ボールのpointercancelでは、その操作で自動移動した選手だけを復元します。`autoMoved` に対象IDを記録し、途中で手動操作を始めた選手は除外します。手動操作の完了時には継続中のボール操作の `autoBefore` をその選手の最新位置へ更新し、再び自動移動してからボールを取り消した場合も手動配置まで戻せるようにします。

`beginManualPosition()` はアニメーション中の選手を実際に表示中の座標から引き継ぎます。`autoPausedAt` に自動移動の停止時刻を保持し、他選手のアニメーションも次のボール移動まで停止します。再開時は停止時間だけ開始時刻をずらします。手動でつかんだ選手はアニメーション管理から外し、終了時に通常のグリッド補正を適用します。キーボードで移動中の選手を操作した場合も表示位置から引き継いでグリッド補正します。

`auto-position.test.cjs` は選択・幾何制約・隙間の改善・再計算の安定性・配置不可能ケースを検証します。`browser.auto.test.cjs` は実際のマウス・タッチ・キーボード操作、履歴、アニメーション途中の手動引き継ぎ、全選手の手動操作、同時タッチ時の停止と再開、手動配置を保つ取り消し、上部表示を検証します。

### 距離によるサイズとFULL

`pieceDiameter(p, ball, adaptive)` はOFFでは従来の前衛0.75m・後衛1m、ON・FULLでは実寸の中心間距離が3m以上で1m、5m以上で1.25mを返します。距離はx座標を9/86、y座標を1/5でmに換算します。ボール自体は0.5mのままです。`updateDisplayedSizes()` と `drawAim()` は同じ表示座標・直径を使用し、両チームと固定選手も更新します。切替やサイズ変更だけで中心を動かさないため、描画時の位置補正は従来の移動範囲を維持し、手動スナップ時は現在の円径を考慮します。

配置候補では各候補の中心間距離から円径を計算します。3m・5mの閾値付近では、円径が変わるとライン接触位置も変わるため、円径と接触位置が一致する候補のみ採用します。FULLでは固定選手を除外せず、横方向はコート全幅を探索します。後続選手を置けない候補を事前除外し、候補間の接触判定表と接線角度を再利用して探索の負荷を減らします。

### 左右位置を維持する10%許容幅

最終候補比較では、現在の全選手のxを保ち、yのみ現在の円径に合うライン接触位置へ合わせた有効な配置を `retained` として作ります。従来の優先順位で選んだ最適候補 `best` に対し、`retained.score[1] <= best.score[1] * 1.1` なら左右を維持します。`score[1]` は空き角度の合計で、最大の隙間や塗り面積ではありません。判定には丸め前の値を使い、浮動小数誤差だけ1e-12まで許容します。最適値0なら現在値も実質0のときだけ維持します。

ON・FULL共通の処理で、許容幅を超えた場合、または現xで円径・接触位置・重なり・グリッド等の制約を満たせない場合は従来どおり最適候補を採用します。現在位置は直前に確定した移動先を指し、アニメーション途中の表示座標ではありません。閾値境界、小さなボール移動の往復、10%を超える隙間、左右を維持したままの前後調整をテストします。
